<div class = "row">
  {{!-- {{#each Posts as |post|}} --}}
    {{!-- {{#if id=}} --}}
                <div class="card">
                    <input class="form-control" id="edit-post-title" type="text" placeholder="{{title}}">
                    <div class="card-body">
                     <div class="form-group">
                      <textarea class="form-control" id="edit-post-textarea" rows="3" placeholder="{{body}}"></textarea>
                     </div>
                    </div>
                    </div>
                    <div class="card-footer text-muted">
                    <button id="edit-post-save-button">Save Post</button>
                    <button id="delete-post-button">Delete Post</button>
                      </div>
                  </div>
            </div>
  {{!-- {{/each}}           --}}
<script src="/js/edit-post.js"></script>
